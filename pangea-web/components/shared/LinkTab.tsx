import Tab from '@mui/material/Tab';
import { NextLinkComposed } from './NextLinkComposed';

export const LinkTab = (props: any) => {
  return <Tab component={NextLinkComposed} {...props} />;
};
export default LinkTab;
